.venv/
__pycache__/
*.pyc
heroes.json
items.json
cache/
*.csv
*.gz
ids.txt
Ids.txt
.agents/
.claude/

# harvested source pages are inputs, not the deliverable --
# rosters.json IS committed on purpose, so it is not listed here
dse_rosters.html
pages/

# the local store is rebuildable from the API — sync it, do not commit it
deadlock.sqlite
deadlock.sqlite-journal
ranks.json